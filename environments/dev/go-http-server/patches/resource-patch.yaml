apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-http-server
spec:
  template:
    spec:
      containers:
      - name: go-http-server
        resources:
          requests:
            cpu: "50m"
            memory: "64Mi"
          limits:
            cpu: "200m"
            memory: "256Mi"
