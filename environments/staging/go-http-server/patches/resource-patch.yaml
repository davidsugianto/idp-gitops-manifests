apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-http-server
spec:
  template:
    spec:
      containers:
      - name: go-http-server
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
