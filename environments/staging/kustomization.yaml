apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: staging

resources:
  # Environment-level resources (shared)
  - namespace.yaml
  - resource-quota.yaml
  - limit-range.yaml
  - network-policy.yaml
  
  # Per-service overlays (will be updated by scaffold script)
  # Add new service here
  - go-http-server
